{
  "name": "header-parser",
  "version": "1.0.0",
  // Set a description for the API's function
  "description": "Lets you get the IP, language & OS for the browser making the request.",
  "main": "server.js", // Set main to "server.js"
  // Define an "engines" obj that holds the node and npm version as strings
  "engines": {
    "node": "4.7.3",
    "npm": "2.15.11"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "M1Gi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2"
  }
}

